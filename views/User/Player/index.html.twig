{% extends 'layouts/master.html.twig' %}

{% block content %}
    <div class="container">
        <h4 class="text-center font-weight-bold mt-4">Jij zit bij tafel:</h4>
        <p class="text-center h4">{{ table_number }}</p>

        <hr>

        <h4 class="text-center font-weight-bold">Waarde fiches</h4>

        <div class="row">
            {% for chip in chips %}
                <div class="col-sm-2">
                    <span class="chip-{{ chip.color }}"></span>
                    <p class="text-center">€ {{ chip.value }},-</p>
                </div>
            {% endfor %}
        </div>

        <hr>

        <h4 class="text-center font-weight-bold">Inkopen en Rebuy</h4>

        <div class="text-center">
            <input type="checkbox" id="inkoop" name="inkoop" value="Inkoop" {% if inkoop_ischecked == true %}checked{% endif %} disabled>
            <label for="inkoop"> Inkoop</label><br>
            <input type="checkbox" id="rebuy" name="rebuy" value="Rebuy" {% if rebuy_ischecked == true %}checked{% endif %} disabled>
            <label for="rebuy"> Rebuy</label><br>
        </div>

        {# <div class="row">
            <div class="col-sm-6">
                <input >
            </div>

            <div class="col-sm-6">

            </div>
        </div> #}
    </div>
{% endblock %}
